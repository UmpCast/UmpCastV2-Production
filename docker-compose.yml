version: "3.9"

services:
  backend:
    build:
      context: backend
      dockerfile: Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./backend:/code    
    ports:
      - "8000:8000"
  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile
    command: yarn start
    volumes:
      - ./frontend/src:/code/src
    ports:
      - "3000:3000"
    depends_on: 
      - backend      
    stdin_open: true